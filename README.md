# Real-Time Weather Analytics

This project streams weather data from OpenWeatherMap API using Kafka (or Redpanda), processes it in Spark Structured Streaming, stores it in PostgreSQL, and visualizes insights in a Streamlit dashboard.
